#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/oem-config-debconf.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall oem-config-debconf
	if grep -qs '^oem-config-debconf$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^oem-config-debconf$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
