#!/bin/sh

set -e

# Automatically added by dh_installinit
if [ "$1" = "purge" ] ; then
	update-rc.d avahi-daemon remove >/dev/null || exit $?
fi
# End automatically added section


if [ "$1" = "purge" ] ; then
        deluser --quiet --system avahi > /dev/null || true
        delgroup --quiet --system avahi > /dev/null || true
fi

