#!/bin/sh
set -e
# Automatically added by dh_gconf
if [ "$1" = remove ] || [ "$1" = upgrade ]; then
	gconf-schemas --unregister gnome-audio-profiles.schemas gnome-sound-recorder.schemas gnome-volume-control.schemas 
fi
# End automatically added section
