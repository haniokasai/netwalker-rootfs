#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/launchpad-integration.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall launchpad-integration
	if grep -qs '^launchpad-integration$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^launchpad-integration$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
