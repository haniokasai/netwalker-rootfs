#! /bin/sh -e

case "$1" in
    configure)
	docdir=/usr/share/doc/openoffice.org-base-core
	if [ -d $docdir ] && [ ! -h $docdir ]; then
	    rm -rf $docdir
	    ln -s openoffice.org-core $docdir
	fi
esac


