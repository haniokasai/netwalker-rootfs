#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/python-pyatspi.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-pyatspi
	if grep -qs '^python-pyatspi$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-pyatspi$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
