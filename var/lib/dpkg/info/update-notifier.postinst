#!/bin/sh

STAMP=/var/lib/update-notifier/dpkg-run-stamp

if [ ! -f $STAMP ]; then
	touch $STAMP
fi

# Automatically added by dh_gconf
if [ "$1" = "configure" ]; then
	gconf-schemas --register update-notifier.schemas 
fi
# End automatically added section

