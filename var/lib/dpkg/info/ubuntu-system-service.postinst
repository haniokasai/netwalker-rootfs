#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/ubuntu-system-service.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall ubuntu-system-service
	if grep -qs '^ubuntu-system-service$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^ubuntu-system-service$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
