#!/bin/sh
set -e
# Automatically added by installdocbase
if [ "$1" = remove ] || [ "$1" = upgrade ] && \
   which install-docs >/dev/null 2>&1; then
	install-docs -r gimp-help-en
fi
# End automatically added section
