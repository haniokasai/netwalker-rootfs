#!/bin/sh
set -e
# Automatically added by dh_gconf
if [ "$1" = remove ] || [ "$1" = upgrade ]; then
	gconf-schemas --unregister gfloppy.schemas gnome-screenshot.schemas gnome-dictionary.schemas gnome-search-tool.schemas gnome-system-log.schemas baobab.schemas 
fi
# End automatically added section
