#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/language-selector.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall language-selector
	if grep -qs '^language-selector$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^language-selector$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
