#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/screen-resolution-extra.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall screen-resolution-extra
	if grep -qs '^screen-resolution-extra$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^screen-resolution-extra$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
