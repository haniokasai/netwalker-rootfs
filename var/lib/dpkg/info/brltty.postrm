#!/bin/sh

set -e

if test "$1" = "purge"
then
	rm -f /etc/brltty.conf
	rm -f /etc/brlapi.key
fi

#update-rc.d brltty remove > /dev/null

# Automatically added by dh_installinit
if [ "$1" = "purge" ] ; then
	update-rc.d brltty remove >/dev/null || exit $?
fi
# End automatically added section

