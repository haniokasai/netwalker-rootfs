#!/bin/sh
set -e
# doc dir is now a symlink to xserver-xorg-input-all
if [ "$1" = upgrade ] && [ ! -L /usr/share/doc/xserver-xorg-input-all ]; then
    rm -rf /usr/share/doc/xserver-xorg-input-all
fi
