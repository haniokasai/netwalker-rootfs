#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/python-uno.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-uno
	if grep -qs '^python-uno$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-uno$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
