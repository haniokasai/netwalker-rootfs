#!/bin/sh
set -e
# Automatically added by dh_gconf
if [ "$1" = remove ] || [ "$1" = upgrade ]; then
	gconf-schemas --unregister panel-toplevel.schemas clock.schemas workspace-switcher.schemas panel-compatibility.schemas panel-global.schemas panel-object.schemas panel-general.schemas fish.schemas window-list.schemas 
fi
# End automatically added section
