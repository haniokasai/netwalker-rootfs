#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/python-launchpad-bugs.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-launchpad-bugs
	if grep -qs '^python-launchpad-bugs$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-launchpad-bugs$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
