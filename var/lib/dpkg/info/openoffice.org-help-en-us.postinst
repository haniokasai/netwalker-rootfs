#! /bin/sh -e

case "$1" in
    configure)
	docdir=/usr/share/doc/openoffice.org-help-en-us
	if [ -d $docdir ] && [ ! -h $docdir ]; then
	    rm -rf $docdir
	    ln -s openoffice.org-l10n-common $docdir
	fi
esac


