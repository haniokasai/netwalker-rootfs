Update Manager for apt
----------------------

This is an application which lets you manage updates available for your
computer via apt and python-apt.

It supports also easy release upgrades. The following environment vars
are honored:

DEBUG_UPDATE_MANAGER: 
- if that is set, debug information is printed to sys.stderr. useful 
  to test e.g. Release Upgrade checking/fetching


